<section class="hero section">
  <div class="container hero-grid">
    <!-- Left: copy -->
    <div class="hero-copy fade-in">
      <span class="eyebrow">⬡ Laboratorio operacional PyME</span>
      <h1 class="h1">
        Tu PyME ya habla.<br />
        <span class="muted">PymIA la escucha con evidencia.</span>
      </h1>
      <p class="lead">
        Convertimos Excels, PDFs, extractos bancarios, ventas, stock y mensajes
        en hallazgos operativos claros. Sin evidencia suficiente, no inventamos.
      </p>
      <div class="hero-actions">
        <a class="btn btn-primary" href="#ingesta">⬆ Subir evidencia</a>
        <a class="btn btn-secondary" href="#como-funciona">Ver cómo funciona</a>
      </div>
      <div class="hero-trust">
        <span class="trust-badge">Sin inventar datos</span>
        <span class="trust-sep">·</span>
        <span class="trust-badge">Evidencia trazable</span>
        <span class="trust-sep">·</span>
        <span class="trust-badge">Hallazgos auditables</span>
      </div>
    </div>

    <!-- Right: terminal panel -->
    <aside class="card hero-terminal">
      <div class="term-topbar">
        <span class="dot dot-red"></span>
        <span class="dot dot-yellow"></span>
        <span class="dot dot-green"></span>
        <span class="term-filename">diagnostico_operativo.pymia</span>
      </div>
      <div class="term-body">
        <div class="term-row">
          <span class="term-key">SÍNTOMA</span>
          <span class="term-val warn">vendo mucho pero no sé si gano</span>
        </div>
        <div class="term-row">
          <span class="term-key">HIPÓTESIS</span>
          <span class="term-val soft">margen / caja / costos</span>
        </div>
        <div class="term-row">
          <span class="term-key">EVIDENCIA REQUERIDA</span>
          <span class="term-val info">ventas + costos + extractos</span>
        </div>
        <div class="term-row">
          <span class="term-key">ESTADO</span>
          <span class="term-val muted">pendiente de evidencia</span>
        </div>
        <div class="term-row">
          <span class="term-key">PRÓXIMO PASO</span>
          <span class="term-val ok">subir planilla de ventas</span>
        </div>
      </div>
      <div class="term-footer">
        <span class="badge badge-muted">Laboratorio activo</span>
        <span class="badge badge-warn">Análisis pendiente</span>
      </div>
    </aside>
  </div>
</section>

<style>
.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  background:
    radial-gradient(ellipse at 8% 6%, rgba(0,230,118,.14) 0%, transparent 38%),
    radial-gradient(ellipse at 92% 90%, rgba(100,181,246,.06) 0%, transparent 32%),
    var(--bg);
  position: relative;
  overflow: hidden;
}
.hero::after {
  content: '';
  position: absolute;
  inset: 0;
  background: repeating-linear-gradient(
    0deg,
    transparent,
    transparent 39px,
    rgba(255,255,255,.018) 40px
  );
  pointer-events: none;
}
.hero-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 56px;
  align-items: center;
  position: relative;
  z-index: 1;
}
.hero-copy {
  display: flex;
  flex-direction: column;
  gap: 24px;
}
.hero-actions {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  margin-top: 4px;
}
.hero-trust {
  display: flex;
  align-items: center;
  gap: 10px;
  flex-wrap: wrap;
  margin-top: 8px;
}
.trust-badge {
  font-family: var(--font-mono);
  font-size: .65rem;
  letter-spacing: .1em;
  text-transform: uppercase;
  color: var(--muted);
}
.trust-sep {
  color: var(--line);
  font-size: .8rem;
}

/* Terminal card */
.hero-terminal {
  padding: 0;
  overflow: hidden;
  position: relative;
}
.term-topbar {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 14px 18px;
  border-bottom: 1px solid var(--line);
  background: var(--panel-2);
}
.dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
}
.dot-red    { background: #ff5f56; }
.dot-yellow { background: #ffbd2e; }
.dot-green  { background: #27c93f; }
.term-filename {
  font-family: var(--font-mono);
  font-size: .7rem;
  color: var(--muted);
  margin-left: 6px;
  letter-spacing: .04em;
}
.term-body {
  padding: 8px 0;
}
.term-row {
  display: grid;
  grid-template-columns: 180px 1fr;
  gap: 12px;
  padding: 14px 20px;
  border-bottom: 1px solid var(--line);
  font-family: var(--font-mono);
  font-size: .75rem;
  align-items: start;
}
.term-row:last-child { border-bottom: none; }
.term-key {
  color: var(--muted);
  font-size: .65rem;
  letter-spacing: .1em;
  text-transform: uppercase;
  padding-top: 2px;
}
.term-val       { color: var(--soft); line-height: 1.5; }
.term-val.warn  { color: var(--yellow); }
.term-val.ok    { color: var(--green); }
.term-val.info  { color: var(--blue); }
.term-val.muted { color: var(--muted); }
.term-val.soft  { color: var(--soft); }

.term-footer {
  display: flex;
  gap: 8px;
  padding: 16px 20px;
  border-top: 1px solid var(--line);
  background: var(--panel-2);
}

@media (max-width: 900px) {
  .hero-grid { grid-template-columns: 1fr; }
  .hero { min-height: auto; padding: 80px 0 64px; }
  .term-row { grid-template-columns: 140px 1fr; }
}
@media (max-width: 620px) {
  .term-row { grid-template-columns: 1fr; gap: 4px; }
  .term-key { font-size: .6rem; }
}
</style>
